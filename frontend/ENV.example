# Copy to .env.local for development

# Base API URL for backend
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Allow admin access without auth (for local testing only)
NEXT_PUBLIC_ALLOW_ADMIN_BYPASS=false
